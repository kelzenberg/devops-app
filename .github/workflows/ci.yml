name: Continuous Integration

on:
  push:
    branches-ignore: [master]
  pull_request:
    types: [reopened]

jobs:
  build-and-test:
    uses: ./.github/workflows/build-and-test.yml
    secrets: inherit
